const validateToken = () => {
    // const tokenValue = localStorage.getItem("token");
    // TODO Call the API to validate the token

    return false;
    // return !!tokenValue;

}

export { validateToken };